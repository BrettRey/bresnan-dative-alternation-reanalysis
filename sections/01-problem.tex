\section{The problem}

TODO: Open from the distinction the project needs: a construction can be rare,
lexically restricted, strongly dispreferred, register-bound, or contrary to a
linguist's informal judgment without thereby falling outside the grammar. The
dative alternation is useful because the choice between the double-object and
prepositional patterns has both a long theoretical history and a reusable
probabilistic dataset.
